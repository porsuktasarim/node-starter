<nav class="navbar navbar-dark bg-danger px-3 py-2">
  <div class="d-flex align-items-center gap-3">
    <a href="/"><img src="<%= ayarlar.logo %>" height="40" alt="Logo"></a>
  </div>

  <div class="d-flex align-items-center flex-grow-1 ms-3">
    <div class="text-white lh-sm">
      <% if (ayarlar.kurumAdi) { %>
        <div class="fw-bold"><%= ayarlar.kurumAdi %></div>
      <% } %>
    </div>
  </div>

  <div class="d-flex align-items-center gap-2 ms-auto">
    <span class="text-white small">
      <i class="bi bi-person-circle me-1"></i>
      <%= kullanici.ad %> <%= kullanici.soyad %>
      <span class="badge bg-light text-danger ms-1"><%= kullanici.rol %></span>
    </span>
    <a href="/profil" class="btn btn-outline-light btn-sm">
      <i class="bi bi-person-circle me-1"></i>Profil
    </a>
    <a href="/cikis" class="btn btn-outline-light btn-sm">
      <i class="bi bi-box-arrow-right me-1"></i>Çıkış
    </a>
  </div>
</nav>